name: Build

on:
    push: ~
    pull_request: ~

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v3

            -
                name: Validate JSON file
                run:
                    sudo apt-get install jsonlint
                    jsonlint data.json
